#include <stdio.h>

/**
 * main - is the entry point of the code
 * 
 * Description: Use the function "printf" to write stdout
 * Return - 0 Successful
*/

int main(void)
{
int i;
for (i = 0; i < 10; i++)
{
printf("%d",i);
}
return (0);
}
